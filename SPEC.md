# Smart Waste Alert Card Feature Specification

## Goal
- สร้างระบบสำหรับแจ้งจุดที่มีขยะหรือถังขยะเต็มภายในมหาวิทยาลัย เพื่อให้เจ้าหน้าที่ได้รับแจ้งและเข้าดำเนินการได้อย่างรวดเร็ว พร้อมนำ AI มาช่วยจำแนกประเภทขยะและวิเคราะห์ระดับความเร่งด่วนในการจัดเก็บจากรูปภาพ

## Requirements

### การแจ้งจุดขยะ
- ผู้ใช้งานสามารถแจ้งจุดที่ต้องการให้เจ้าหน้าที่เก็บขยะได้ โดยไม่ต้อง login
- ผู้ใช้งานระบุสถานที่ได้ 2 ทาง:
  1. สแกน QR Code ที่ติดอยู่กับจุด/ถังขยะ ระบบดึง `location_id` ที่ผูกไว้ล่วงหน้าอัตโนมัติ (ชื่อจุด + lat/lng)
  2. เข้าฟอร์มโดยตรง (ไม่ผ่าน QR) ให้เลือกจุดจากรายการ/แผนที่ที่ลงทะเบียนไว้ในระบบเท่านั้น (dropdown หรือ pin บนแผนที่) ห้ามพิมพ์สถานที่เป็น free text ล้วน เพื่อกันข้อมูลเพี้ยนหรือจุดไม่มีอยู่จริง
  3. หากสแกน QR ที่ `location_id` ไม่ตรงกับรายการจุดที่ลงทะเบียนไว้ ระบบต้องปฏิเสธและแสดง error "ไม่พบจุดนี้ในระบบ กรุณาติดต่อเจ้าหน้าที่"
- ผู้ใช้งานสามารถกรอกรายละเอียดเพิ่มเติมได้ (field `note`, type: text, optional, จำกัดไม่เกิน 500 ตัวอักษร)
- ผู้ใช้งานสามารถแนบรูปภาพขยะได้ อย่างน้อย 1 รูป อย่างมาก 3 รูปต่อรายการแจ้ง
  - ชนิดไฟล์ที่รับ: `.jpg`, `.jpeg`, `.png`, `.webp` เท่านั้น ตรวจสอบทั้งนามสกุลไฟล์และ MIME type จริง (ไม่เชื่อ extension อย่างเดียว)
  - ขนาดไฟล์สูงสุด 5MB ต่อรูป เกินกว่านี้ปฏิเสธพร้อมข้อความแจ้งเหตุผล

### AI วิเคราะห์ภาพ
- AI ช่วยวิเคราะห์รูปภาพและจำแนกประเภทขยะ เช่น
  - ขยะทั่วไป
  - ขยะรีไซเคิล
  - ขยะอินทรีย์
  - ขยะอันตราย
- AI ช่วยวิเคราะห์ระดับความเร่งด่วนในการจัดเก็บขยะจากภาพ โดยใช้เกณฑ์เชิงปริมาณ (ประมาณสัดส่วนพื้นที่ถัง/บริเวณที่มีขยะปกคลุมจากภาพ):
  - 🔴 `เร่งด่วน` — ขยะปกคลุม/ล้นเกิน 80% ของถังหรือพื้นที่ในภาพ
  - 🟠 `ควรดำเนินการ` — ขยะปกคลุม 50–80%
  - 🟢 `ไม่เร่งด่วน` — ขยะปกคลุมต่ำกว่า 50%
  - ค่า threshold ปรับได้ผ่านไฟล์ config โดย admin ไม่ hardcode ในโค้ด
- แสดงผลการวิเคราะห์ประเภทขยะและระดับความเร่งด่วนให้ผู้ใช้งานทราบทันทีหลังอัปโหลด
- หาก AI ไม่สามารถจำแนกประเภทขยะ หรือประเมินระดับความเร่งด่วนได้ ให้บันทึกค่านั้นเป็น `unclassified` ไม่บล็อกการ submit ของผู้ใช้ ให้เจ้าหน้าที่ตรวจสอบและกำหนดค่าด้วยมือภายหลัง

### บันทึกและสถานะ
- บันทึกรายการแจ้งลงในไฟล์ `waste-reports.json` โดยเขียนผ่าน file lock ทีละครั้ง (เขียนเสร็จค่อยปล่อย lock ให้คำขอถัดไป) เพื่อกันข้อมูลชนกันเมื่อมีการแจ้งพร้อมกันหลายรายการ
- ก่อนเขียนทับไฟล์ทุกครั้ง ให้สำรอง (backup) ข้อมูลเดิมไว้ 1 ชุดล่าสุด หากไฟล์หลักเสียหาย/parse ไม่ได้ ระบบต้อง fallback ไปใช้ backup พร้อมแจ้งเตือน admin
- แต่ละรายการมีสถานะ ได้แก่ `รอดำเนินการ`, `กำลังดำเนินการ` และ `ดำเนินการเสร็จสิ้น`
- การเปลี่ยนสถานะเดินหน้าทางเดียวเท่านั้น: `รอดำเนินการ` → `กำลังดำเนินการ` → `ดำเนินการเสร็จสิ้น` ไม่รองรับการย้อนสถานะกลับ (หากกดผิดต้องสร้างรายการใหม่แทน)
- แสดงวันและเวลาที่รับแจ้งและดำเนินการ (timezone Asia/Bangkok, format ISO 8601 พร้อม offset)

### การแจ้งเตือน
- เมื่อมีรายการใหม่ ระบบต้องส่งแจ้งเตือนเข้ากลุ่ม LINE กลางของเจ้าหน้าที่ทั้งมหาวิทยาลัย ภายใน 10 วินาทีหลังบันทึกรายการสำเร็จ (ใช้ event-based push ไม่ใช่ polling)
- หากส่งแจ้งเตือนไม่สำเร็จ ให้ retry อัตโนมัติ 3 ครั้งแบบ exponential backoff หากยังไม่สำเร็จ บันทึก error log และแสดงสถานะ "แจ้งเตือนล้มเหลว" บน dashboard ให้เจ้าหน้าที่เห็น
- หาก AI วิเคราะห์ว่าเป็นรายการ `เร่งด่วน` ระบบต้องแสดงระดับความเร่งด่วนให้เจ้าหน้าที่เห็นอย่างชัดเจน (เช่น ไฮไลต์สีแดง เรียงลำดับบนสุดของรายการ)

### สิทธิ์การใช้งานเจ้าหน้าที่
- เจ้าหน้าที่เข้าดูรายการแจ้งและเปลี่ยนสถานะได้โดยไม่ต้อง login (เปิดกว้างสำหรับทุกคนในทีม)
- เมื่อเปลี่ยนสถานะ มี field `changed_by` (free text, optional) ให้กรอกชื่อผู้ดำเนินการ เพื่อประโยชน์ในการติดตามงาน แม้ไม่ได้บังคับด้วยระบบ auth

### ทั่วไป
- ระบบต้องใช้งานง่าย รองรับโทรศัพท์มือถือ และตอบสนองรวดเร็ว โดยมีเกณฑ์วัดผลชัดเจน:
  - เวลาโหลดหน้าฟอร์มแรก (First Contentful Paint) ไม่เกิน 2 วินาที บนเครือข่าย 4G
  - รองรับ responsive breakpoint อย่างน้อยที่ 375px, 768px, 1024px

## Tech Stack
- Node.js
- Express
- HTML / CSS / JavaScript
- JSON สำหรับจัดเก็บข้อมูล (พร้อม file lock และระบบ backup อัตโนมัติ)
- QR Code
- AI สำหรับช่วยจำแนกประเภทขยะจากรูปภาพ
- AI สำหรับช่วยวิเคราะห์ระดับความเร่งด่วนในการจัดเก็บ
- LINE Messaging API สำหรับแจ้งเตือนเจ้าหน้าที่ (กลุ่มกลาง)

## Security & AI Ethics
- จำกัดประเภทไฟล์รูปภาพที่อัปโหลดได้เฉพาะ `.jpg`, `.jpeg`, `.png`, `.webp` และขนาดไม่เกิน 5MB ต่อไฟล์ ตรวจสอบ MIME type จริงของไฟล์ ไม่เชื่อนามสกุลไฟล์อย่างเดียว
- ไม่เก็บข้อมูลส่วนบุคคลที่ไม่จำเป็น หากภาพที่อัปโหลดมีใบหน้าบุคคลติดมาโดยไม่ตั้งใจ ระบบทำ face-blur อัตโนมัติก่อนบันทึกไฟล์ทุกครั้ง
- ผลการจำแนกประเภทขยะและระดับความเร่งด่วนจาก AI เป็นเพียงคำแนะนำจาก AI ไม่ใช่ผลตัดสินสุดท้าย
- ผู้ใช้งานหรือเจ้าหน้าที่สามารถตรวจสอบและแก้ไขผลการวิเคราะห์ของ AI ได้
- การตัดสินใจดำเนินการขั้นสุดท้ายยังเป็นหน้าที่ของเจ้าหน้าที่
- ไม่เปิดเผยข้อมูลภายในระบบแก่บุคคลที่ไม่เกี่ยวข้อง ไฟล์ `waste-reports.json` ต้องไม่ถูก serve เป็น static file ผ่าน URL ตรง เข้าถึงได้เฉพาะผ่าน API ของระบบเท่านั้น
- ทุก field ข้อความที่ผู้ใช้กรอก (เช่น `note`, `changed_by`) ต้อง sanitize/escape ก่อนแสดงผลบน dashboard เพื่อป้องกัน stored XSS
- QR Code ต้องผูกกับ `location_id` ที่ลงทะเบียนไว้ล่วงหน้าในระบบเท่านั้น สแกนแล้วต้อง validate กับรายการจุดที่มีอยู่จริง ป้องกัน QR ปลอม/โคลนนิ่งพาไปฟอร์มอื่น
- จำกัดอัตราการส่งแจ้งจากผู้ใช้แต่ละคน/IP ไม่เกิน 5 รายการต่อชั่วโมง เพื่อป้องกัน spam และ DoS ผ่านการอัปโหลดภาพจำนวนมาก

## Edge Cases
- ผู้ใช้งานไม่ได้ระบุสถานที่ → ปฏิเสธการ submit พร้อมแจ้งเตือนในฟอร์ม (สถานที่เป็น field บังคับ)
- ผู้ใช้งานไม่ได้กรอกรายละเอียด → อนุญาต (field `note` เป็น optional)
- ผู้ใช้งานไม่ได้แนบรูปภาพ → ปฏิเสธการ submit พร้อมแจ้งเตือนในฟอร์ม (ต้องมีอย่างน้อย 1 รูป)
- AI ไม่สามารถจำแนกประเภทขยะได้ → บันทึกรายการด้วยค่าประเภทขยะ `unclassified` ไม่บล็อกการ submit ให้เจ้าหน้าที่จำแนกด้วยมือภายหลัง
- AI ไม่สามารถประเมินระดับความเร่งด่วนได้ → บันทึกรายการด้วยค่าความเร่งด่วน `unclassified` ให้เจ้าหน้าที่ประเมินด้วยมือ ไม่บล็อกการ submit
- รูปภาพไม่ชัดเจนหรือไม่พบขยะในภาพ → แสดงผลแจ้งผู้ใช้ว่า "ไม่พบขยะในภาพ กรุณาถ่ายใหม่หรือยืนยันส่งต่อ" ให้เลือกส่งต่อได้โดยรายการจะถูกทำเครื่องหมายให้เจ้าหน้าที่ตรวจสอบซ้ำ
- ไม่พบไฟล์ `waste-reports.json` → สร้างไฟล์ใหม่ว่างอัตโนมัติ (โครงสร้าง `[]`) และบันทึก log แจ้ง admin
- ไฟล์ `waste-reports.json` เสียหาย/parse ไม่ได้ → fallback ไปใช้ไฟล์ backup ล่าสุด พร้อมแจ้ง error ให้ admin ตรวจสอบ
- ไม่สามารถส่งการแจ้งเตือนไปยังผู้รับผิดชอบได้ → retry 3 ครั้งแบบ exponential backoff หากยังไม่สำเร็จ บันทึก error log และแสดงสถานะ "แจ้งเตือนล้มเหลว" บน dashboard
- ผู้ใช้งานส่งข้อมูลซ้ำ → ตรวจจับด้วย dedup key (`location_id` + ภายในช่วงเวลา 30 นาที) หากซ้ำ ให้รวมเข้ารายการเดิมและเพิ่มค่า `report_count` แทนการสร้างรายการใหม่ พร้อมแจ้งผู้ใช้ว่า "มีรายการแจ้งอยู่แล้วสำหรับจุดนี้"

## Scalability

- สามารถเพิ่ม QR Code สำหรับจุดทิ้งขยะหรือถังขยะต่าง ๆ ภายในมหาวิทยาลัย โดยลงทะเบียน `location_id` ใหม่ผ่านหน้า admin ก่อนสร้าง QR
- สามารถเพิ่มผู้รับผิดชอบแยกตามพื้นที่ได้ในอนาคต (ปัจจุบันแจ้งเตือนรวมเข้ากลุ่มกลางกลุ่มเดียว)
- สามารถพัฒนา Dashboard เพื่อดูจำนวนการแจ้ง ระดับความเร่งด่วน และระยะเวลาในการจัดเก็บขยะ
- สามารถจัดลำดับรายการให้เจ้าหน้าที่เห็นจุดที่เร่งด่วนก่อน โดยเรียงตามระดับความเร่งด่วน (เร่งด่วน > ควรดำเนินการ > ไม่เร่งด่วน) แล้วตามด้วยเวลาที่แจ้งเก่าสุดก่อน
- สามารถนำข้อมูลไปวิเคราะห์จุดที่มีปัญหาขยะสะสมบ่อย เพื่อช่วยวางแผนรอบและความถี่ในการจัดเก็บขยะในอนาคต
