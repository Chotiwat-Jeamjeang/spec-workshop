ANTHROPIC_API_KEY=
PORT=3000
# NEW for Phase 1 — generate via: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
QR_SIGNING_SECRET=
# Optional — base URL encoded into generated QR PNGs, defaults to http://localhost:3000
QR_BASE_URL=
